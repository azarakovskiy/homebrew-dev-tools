# Go Code Style

- Always keep code `gofmt`-clean.
- Keep package names short, lower-case, and non-stuttering.
- Keep identifier names idiomatic Go (`ID`, `URL`, `HTTP`, `JSON`).
- Group imports as stdlib, third-party, local module.
- Add doc comments for exported identifiers.
